[defaults]
# Deprecation warnings come mostly from upstream collections (e.g. community.docker
# used by the molecule docker driver) that we cannot fix here. Suppress them so
# the actual play output stays readable.
deprecation_warnings = False
inventory = inventory.yml
vault_password_file = .vault_pass
