---
ssh_port: "{{ vault_ovh_portfolio_port }}"
ssh_allow_users: "{{ vault_ovh_portfolio_user }}"
ssh_max_auth_tries: 5
ssh_login_grace_time: 30
ssh_client_alive_interval: 120
ssh_client_alive_count_max: 2
